# 项目规则

本文件约束本项目之后所有 Codex 任务的执行方式。开始任务前先阅读并遵守以下规则。

1. 诚实作答：不确定就明确回答“不确定”，不编造、不猜测、不输出没有依据的结果。
2. 任务完成后再回复：不要中途只回复一句对话就停下，应持续执行到任务完成，或遇到必须由用户处理的阻塞后再回复。
3. 减少中断：不要频繁中断思考或暂停任务，不要等待用户反复催促；能一次完成的工作就一次完成。
4. 保护隐私：不泄露用户隐私、密钥、个人敏感信息等不应公开的内容。
5. 如实呈现结果：不美化、不夸大、不添加未经验证的内容；结果未达标就明确说明未达标，不得把未达标说成达标。
6. 分支工作流：每个新任务先新建独立功能分支完成，不同任务之间创建不同的分支，不要把所有任务堆在同一分支；完成后等待用户确认，再合并回 `main` 主线；未经确认不得合并；确认合并后删除已完成提交的当前功能分支；提交时只包含当前任务相关文件，不得夹带其他任务的未提交改动。
7. 禁止“准备做”式回复中断：所有“准备做”的任务不允许通过回复中断任务进程，只有任务执行完成或遇到报错必须中断时才能回复。
8. GitHub 版本更新规则：以后任何涉及版本号更新的任务，必须一次性同步完成以下事项：
   - 更新版本号：默认遵循语义化版本，新增功能提升 minor（如 `0.2.4` → `0.3.0`），修复提升 patch（如 `0.3.0` → `0.3.1`）；不主动升到 `1.0.0` 及以上，除非用户明确要求。
   - 更新代码版本信息：同步修改 `src/docking/__init__.py` 的 `__version__`。
   - 更新说明文档：同步更新 `README.md` 中的版本说明与本次变更说明。
   - 更新 Git Tag：创建或更新对应 `vX.Y.Z` Tag，并推送到 GitHub。
   - 更新 GitHub Release：创建或更新对应 Release，Release 说明需与本次变更一致；低于 `1.0.0` 时默认使用 Pre-release，不标记为 Latest，除非用户明确要求正式发布。
9. 多任务并行：多个任务同时执行时，各任务完成自己的分支并合并即可，不需要推送；等待所有分支任务完成并经用户确认后，再统一推送到 GitHub。
10. 版本更新同步：涉及版本号更新的任务，必须一次性同步更新代码文件、`README.md`、Git Tag 与 GitHub Release 等全部相关内容，不得只更新其中一部分。
11. 完成答复前自检：每次完成任务准备答复前，先自动检查本次执行是否完全符合当前所有规则；若有未遵循的规则，必须先修正并完成任务，不得直接结束任务进行答复。尤其注意检查第 2、3、7 条。
12. commentary 防重复：每条 commentary 只发送一次，禁止在连续更新中重复相同或高度相似的句子；若上一条 commentary 已包含相同内容，必须停止重复并等待下一步。
